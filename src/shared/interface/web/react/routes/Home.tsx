import React from 'react';
import { HomeView } from '@/app/HomeView';

export const Home: React.FC = () => {
    return (
        <div>
            <HomeView projects={[]} />
        </div>
    );
};
